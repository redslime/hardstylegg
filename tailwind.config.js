/** Tailwind v4 configuration (kept minimal).
 * DaisyUI is registered via the CSS `@plugin` API in assets/main.css.
 * Content scanning is handled by the Vite plugin; no `content` key is required.
 */
export default {
  theme: {
    extend: {
    },
  },
}